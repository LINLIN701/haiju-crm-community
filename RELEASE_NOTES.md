# V1.01.01 跨行业关系维护与中文日志

- 从仅零售定位调整为跨行业关系维护：个人/机构、行业、所属机构、职务、邮箱、关系类型、阶段和关注事项。
- 新字段接通新增、编辑、详情、行业/阶段筛选、搜索、CSV与AI人工核对；旧CSV可继续导入，旧API省略relationship时保留已有新字段。
- 关系概览展示档案、到期跟进和未来联系；消费改为可选展开，不作为各行业共同指标。
- 历史和新增操作日志统一中文显示，保留原英文审计代码；未知动作显示“其他操作”。
- 新增[行业应用说明](docs/INDUSTRY_SCENARIOS.md)，更新真实截图与[使用教程](docs/USER_GUIDE.md)。

包含V2增量数据库迁移，保留旧客户、跟进、消费与日志。升级前备份数据库和环境配置，保持原Compose项目名和卷。**不能直接以V1.00.02代码回退V2数据库**；需使用迁移前备份恢复到独立环境并验证后切换。正式数据库操作需由部署管理员明确确认。

本版前端39个模块构建与生产依赖审计通过，后端10项测试通过；真实MySQL 8.4验证旧V1升V2、关系字段保存/筛选/CSV、匿名401、AI未配置503及中文历史日志。真实浏览器已验证建档、编辑、跟进、组合筛选、错误重试和手机布局，并重拍7张教程截图。重启及独立库备份恢复后，客户/跟进/消费/操作日志/AI日志计数4/2/1/8/2一致，关系字段与跟进内容指纹一致。

本版是跨行业关系维护底座，不是完整保险、银行或地产业务系统。公网HTTPS签发、真实付费模型成功解析及完整镜像CVE扫描仍未实测；现有日期时区一致性也需部署者核对，不应作为精准金融业务时点凭据。GitHub CI结果以本次Release为准。

## V1.00.02 部署与使用教程

- 新增[部署教程](docs/DEPLOYMENT.md)：Windows/Ubuntu准备、配置、启动、HTTPS、AI、备份恢复、升级和排错。
- 新增[使用教程](docs/USER_GUIDE.md)：7张真实截图、虚构客户流程和可下载CSV样例。
- README首屏增加教程入口；公开版本和页面标识同步V1.00.02。
- 明确CSV最近500条限制、提醒口径、删除级联及AI人工确认边界。

本版没有业务规则变更、没有新Flyway迁移。已有部署沿用原数据库卷和项目名；升级前仍应保留备份。教程已在隔离环境执行真实页面与备份恢复演练；公网证书签发、真实付费模型成功抽样没有实测。

## V1.00.01 首次社区发布

这是海聚客户管理系统社区版的首个公开版本，提供可独立构建、独立部署的单管理员客户管理内核。

## 已包含

- 客户档案、标签、搜索、手机号重复保护。
- CSV 导入导出及公式注入保护。
- 跟进记录、下次跟进计划、消费记录和经营概览。
- 通知、操作日志和移动端基础界面。
- 用户自备模型密钥的真实外部模型文字解析；结果必须人工确认后保存，外部模型不可用时明确失败。
- Docker Compose + MySQL 8.4 一键启动方案。

## 安全提醒

- 首次启动前必须修改数据库与管理员密码，并为生产环境配置 HTTPS。
- 社区数据库必须是全新独立数据库，不得指向其他业务系统的生产库。
- `.env`、客户数据、日志、备份和模型密钥不得提交到代码仓库。

完整的社区/商业边界见 [COMMERCIAL_EDITIONS.md](COMMERCIAL_EDITIONS.md)，升级前请阅读 [SECURITY.md](SECURITY.md)。

## 验证与限制

- 前端生产构建、依赖审计和后端 4 项自动化测试通过。
- 临时 MySQL 8.4 空库迁移与 Docker Compose 实际 HTTP 验证通过。
- AI 未配置时明确失败已验证；本次发布未使用付费外部模型执行真实解析抽样。
- 容器基础镜像已核对版本及许可证，完整镜像 CVE 扫描尚未完成。
